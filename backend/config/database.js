/**
 * Database configuration (placeholder for future database integration)
 */

// This file can be used for future database configurations
// Currently using in-memory storage (Maps) for simplicity

const config = {
    // Database connection settings would go here
    development: {
        // Example: MongoDB connection string
        // mongoUri: process.env.MONGODB_URI || 'mongodb://localhost:27017/video-conferencing'
    },
    production: {
        // Production database settings
    }
};

module.exports = config;